package com.drdisagree.teledrive.presentation.onboarding

/** Whether the dialling codes are on their way, usable, or unavailable. */
enum class CountryLoadState {
    LOADING,
    READY,
    FAILED
}
